import React from "react";
import "../App.css";
const TodoCard = (todos) => {
  const { id, title, completed, dueDate } = todos;

  return (
    <div>     
    </div>
  );
};

export default TodoCard;
